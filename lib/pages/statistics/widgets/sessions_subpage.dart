import 'package:flutter/material.dart';

class SessionsSubpage extends StatelessWidget {
  const SessionsSubpage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        SizedBox(height: 10),


      ],
    );
  }
}
